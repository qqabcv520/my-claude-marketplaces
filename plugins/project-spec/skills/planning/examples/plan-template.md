# [功能名称] 实施计划

> 计划日期：YYYY-MM-DD HH:mm
> 复杂度：简单 / 中等 / 复杂 / 系统级

---

## 1. 背景与目标

**问题/需求：**
[描述要解决的问题或要实现的需求]

**目标：**
- [目标1]
- [目标2]

**成功标准：**
- [量化的成功指标1]
- [量化的成功指标2]

---

## 2. 需求概览

**要做的事：**
- [功能点1]
- [功能点2]
- [功能点3]

**不做的事：**
- [明确排除的功能] — [理由]

**核心流程：**
- [用户操作路径/状态流转描述]

**业务规则与边界：**
- [关键业务规则和限制条件]

### 功能分解树（复杂/系统级）

```
Epic: [一级模块名]
├── Feature: [二级功能]
│   ├── Story: [具体行为]
│   └── Story: [具体行为]
└── Feature: [二级功能]
    ├── Story: [具体行为]
    └── Story: [具体行为]
```

### 2.4 用户故事级验收条件（复杂/系统级）

- [用户能通过XXX完成XXX]
- [当XXX时用户能看到XXX]
- [XXX失败时用户能XXX]

---

## 3. 探索发现

### 3.1 关键文件

| 文件路径 | 说明 |
|----------|------|
| `path/to/file` | [文件作用] |
| `path/to/file` | [文件作用] |

### 3.2 现有模式与可复用资源

- [可复用的函数/模块/模式及其路径]
- [现有的约定和规范]

### 3.3 依赖关系

- [相关模块/服务的依赖]

---

## 4. 方案设计

### 4.1 方案概述

[一段话描述推荐的技术方案]

### 4.2 方案对比（如有多个方案）

| 维度 | 方案A: [名称] | 方案B: [名称] |
|------|---------------|---------------|
| 实现复杂度 | [评估] | [评估] |
| 性能 | [评估] | [评估] |
| 可维护性 | [评估] | [评估] |
| 兼容性 | [评估] | [评估] |
| 安全性 | [评估] | [评估] |
| 成本 | [评估] | [评估] |

**推荐方案：** [方案名称] — [推荐理由]

### 4.3 模块规约（复杂/系统级）

#### 模块: [模块名]

**数据模型：**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | string | PK, UUID | 唯一标识 |
| name | string | NOT NULL, max 100 | 名称 |

**接口契约：**

```
POST /api/resource
入参: { name: string, type: enum("A","B") }
出参: { id: string, name: string, createdAt: string }
错误码: 400(参数无效), 409(名称重复), 500(服务器错误)
```

**业务规则：**

- if [条件A] then [行为1] else [行为2]
- if [条件B] and [条件C] then [行为3]

### 4.4 技术性验收标准（复杂/系统级）

**[Story名称]：**

- Given [前置条件] / When [操作] / Then [预期结果]
- Given [前置条件] / When [异常操作] / Then [错误处理结果]

---

## 5. 执行步骤> 每个步骤必须自包含：假设执行者对代码库零了解，仅凭本计划即可准确执行。
> 禁止用描述代替代码（如"添加验证逻辑"），必须给出完整的、可直接使用的代码片段。

### 步骤1: [步骤名称]

**目标：** [步骤目标]

**涉及文件：**
- 新增: `exact/path/to/new-file.ts`
- 修改: `exact/path/to/existing.ts`
- 测试: `tests/exact/path/to/test.ts`

**操作：**

1. 在 `exact/path/to/existing.ts` 中 [具体位置描述，如"在 `export class Foo` 类内末尾"] 添加：

```typescript
// 完整的、可直接使用的代码
function example(): string {
  return "hello";
}
```

2. 创建 `exact/path/to/new-file.ts`：

```typescript
// 完整的文件内容
export function newFeature(): void {
  // ...
}
```

**验证：**
- 运行: `npm test -- tests/exact/path/to/test.ts`
- 预期: 所有测试通过

### 步骤2: [步骤名称]

**目标：** [步骤目标]

**涉及文件：**
- 修改: `exact/path/to/file.ts`

**操作：**

1. [具体操作，包含完整代码片段]

**验证：**
- 运行: `[精确的验证命令]`
- 预期: `[预期输出或结果]`

### 步骤依赖

| 步骤 | 依赖 |
|------|------|
| 步骤1 | - |
| 步骤2 | - |
| 步骤3 | 步骤1, 步骤2 |
| 步骤4 | 步骤3 |

---

## 6. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| [风险描述] | 高/中/低 | 高/中/低 | [预防和应对措施] |
| [风险描述] | 高/中/低 | 高/中/低 | [预防和应对措施] |

---

## 7. 测试策略

| 层次 | 覆盖范围 | 说明 |
|------|----------|------|
| 单元测试 | [覆盖哪些模块/函数] | [测试方法] |
| 集成测试 | [覆盖哪些流程] | [测试方法] |
| E2E测试 | [覆盖哪些场景] | [测试方法] |

**端到端验证步骤：**
1. [验证步骤1]
2. [验证步骤2]
3. [验证步骤3]
